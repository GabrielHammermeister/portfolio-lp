import {{pascalCase name}} from './{{pascalCase name}}.index'
import { Meta, Story } from '@storybook/react'

export default {
   title: '{{pascalCase name}}',
   component: {{pascalCase name}}
} as Meta

export const Basic: Story = (args) => <{{pascalCase name}} {...args} />

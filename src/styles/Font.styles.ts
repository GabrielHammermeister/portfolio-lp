import { createGlobalStyle } from 'styled-components'

export const FontStyles = createGlobalStyle`

   body {
      font-family: Heebo;
      font-style: normal;
      letter-spacing: 0px;
      text-align: center;
   }

   h1 {

      font-size: 32px;
      font-weight: 700;
      line-height: 40px;

   }

   p {
      font-size: 16px;
      font-weight: 400;
      line-height: 24px;
   }
`
